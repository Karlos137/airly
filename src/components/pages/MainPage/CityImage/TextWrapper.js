import styled from "styled-components";

const TextWrapper = styled.div`
  position: absolute;
  text-align: center;
  bottom: calc(50% - 30px);
  left: calc(50% - 56px);
`;

export default TextWrapper;
